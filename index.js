/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import ItemDisplayScreen from './screens/ItemDisplayScreen'
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ItemDisplayScreen); //App);
